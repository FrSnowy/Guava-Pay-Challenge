.container {
  position: fixed;
  top: 16px;
  left: 0;
  width: 320px;
  height: calc(100% - 32px);
  padding: 40px 24px;

  border-right: 1px solid rgba(0, 0, 0, .25);
  display: flex;
  flex-direction: column;
  overflow-x: hidden;
  overfloy-y: auto;
}